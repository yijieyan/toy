<!DOCTYPE html>
<html>
  <head>
    <style>
      body,html {
        margin:0;
        padding: 0;
        color:#666;
      }
      h4 {
        text-align: center;
      }
      h4 em {
        color:red;
      }
      p {
        text-align: center;
      }
      .weather {
        margin-top: 50px;
        text-align: center;
      }
      .weather span {
        margin-left:30px;
      }
      .weather span img {
        width: 30px;
        height:30px;
        vertical-align: bottom;
      }
    </style>
  </head>
  <body>
    <h4>今天是我们认识的<em><%= days %></em>天</h4>
    <div class="weather">
        <%= date%> <span> <img src="<%=img%>"/></span>
        <span><%=temperature%></span>
        <span>
          <% if (epollution_count <= 50) {%> 
            <b style="color:#04fd03;"><%=epollution_count%></b> 
          
          <% } else if (epollution_count >= 51 && epollution_count <= 100) {%> 
            <b style="color:#fbff04;"><%=epollution_count%></b>
          
          <% } else if (epollution_count >= 101 && epollution_count <= 150) {%>
            <b style="color:#fd9a00;"><%=epollution_count%></b>
          
          <% } else if (epollution_count >= 151 && epollution_count <= 200) {%> 
            <b style="color:#fc0108;"><%=epollution_count%></b> 
          
          <% } else if (epollution_count >= 201 && epollution_count <= 300) {%>
            <b style="color:#ca66fa;"><%=epollution_count%></b>
          
          <% } else {%> 
            <b style="color:#da0097;"><%=epollution_count%></b>
          <% } %>
          <%=epollution_level%>
        </span>
    </div>
  </body>
</html>
